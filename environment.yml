name: bluesky-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pandas
  - pytest
  - pyomo
  - sympy
  - matplotlib
  - networkx
  - pip
  - pip:
      - mypy
      - ruff
      - pytest-cov
